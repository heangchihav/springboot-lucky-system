# ==========================
# Application Security (prod)
# ==========================

# JWT secret (must be at least 32 characters)
JWT_SECRET=your-256-bit-secret-key-change-in-production-minimum-32-chars

# Cookie flags
# true in production (HTTPS required)
COOKIE_SECURE=true

# Fail closed if Redis-backed security checks are unavailable
SECURITY_FAIL_CLOSED=true

# Reject suspicious JTI usage (e.g., IP prefix change) and blacklist the JTI
SECURITY_JTI_REJECT_SUSPICIOUS=true
