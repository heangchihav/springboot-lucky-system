package com.example.callservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RestTemplateConfig {
    // RestTemplate bean is defined in AppConfig.java to avoid conflicts
}
