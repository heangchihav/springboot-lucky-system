package com.example.marketingservice.repository.area;

import com.example.marketingservice.entity.area.MarketingArea;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MarketingAreaRepository extends JpaRepository<MarketingArea, Long> {
}
